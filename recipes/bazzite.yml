name: bazzite-legion
base-image: ghcr.io/ublue-os/bazzite
image-version: stable
description: Custom Bazzite for Lenovo Legion Laptops

modules:
  - type: script
    scripts:
      - install-legion.sh

  - type: dnf
    repos:
      copr:
        - jhyub/supergfxctl-plasmoid
    install:
      packages:
        - qemu-kvm
        - libvirt
        - edk2-ovmf
        - supergfxctl-plasmoid

  - type: systemd
    system:
      enabled:
        - plasmavantage-noroot.service
